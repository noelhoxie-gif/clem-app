# Deployed Clem backend URL — required for native API calls (color analysis, etc.)
# Example: https://your-app.lovable.app
VITE_SERVER_BASE_URL=

# Server-only (not bundled into mobile app)
LOVABLE_API_KEY=
